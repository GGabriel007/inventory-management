/* Fields

    name -> String, required
    location -> String, required
    capacity -> Number, required (max capacity)
    currentInventory -> Number, default 0 (to track how full the warehouse is)
    createdAt -> Date, default now

*/
